name: .NET Core

on:
  pull_request:
    branches: [ master ]

jobs:
  build_and_punlish:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.101
    - name: Install dependencies
      run: |
        dotnet restore Ac682.Hyperai.Clients.Mirai
    - name: Build
      run: |
        dotnet build --configuration Release --no-restore Ac682.Hyperai.Clients.Mirai
    - name: publish on version change
      id: publish_nuget
      uses: rohith/publish-nuget@v2
      with:
        # Filepath of the project to be packaged, relative to root of repository
        PROJECT_FILE_PATH: Ac682.Hyperai.Clients.Mirai/Ac682.Hyperai.Clients.Mirai.csproj
        # API key to authenticate with NuGet server
        NUGET_KEY: ${{ secrets.PACKAGE_PUSH }}
        INCLUDE_SYMBOLS: true